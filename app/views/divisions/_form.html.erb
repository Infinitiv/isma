<%= form_for(@division) do |f| %>
  <% if @division.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@division.errors.count, "error") %> prohibited this division from being saved:</h2>

      <ul>
      <% @division.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label 'Адрес' %><br />
    <%= f.text_field :location_verbose %>
  </div>
  <div class="field">
    <%= f.label 'Широта' %><br />
    <%= f.text_field :location_geo_latitude %>
  </div>
  <div class="field">
    <%= f.label 'Долгота' %><br />
    <%= f.text_field :location_geo_longitude %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
