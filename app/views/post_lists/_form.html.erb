<%= form_for(@post_list) do |f| %>
  <% if @post_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_list.errors.count, "error") %> prohibited this post_list from being saved:</h2>

      <ul>
      <% @post_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
